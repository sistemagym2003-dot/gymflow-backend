export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
      <h1>GymFlow API</h1>
      <p>Backend solo API. Consume los endpoints bajo <code>/api</code>.</p>
    </main>
  );
}
